# Firmware — artefactos

Binarios (`.bin`), checksums (`.sha256`) y manifests OTA (`manifest*.json`).

Notas de versión, release notes e instrucciones OTA:

→ [docs/00 - docref/versiones/](../docs/00%20-%20docref/versiones/)
